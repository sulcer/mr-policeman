import React from 'react';
import { Text, View } from 'react-native';

const Onboarding = () => {
  return (
    <View className={'flex-1 justify-center items-center'}>
      <Text className={'text-gray-500'}>Onboarding Screen</Text>
    </View>
  );
};

export default Onboarding;
